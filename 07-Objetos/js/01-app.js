// crear objetos en javascript
// un objeto nos permite agrupar todo en una sola variable

// es muy importante tener en cuenta, que a esto se le conoce como "object literal"
const producto = {
    // un objeto tiene la sintaxis de brackets, por lo que cada que se vea esta sintaxis. Quiere decir que se trata de un objeto
    // dentro de un objeto para declarar valores, lo hacemos atraves de una propiedad o llave y su valor
    nombre: 'Monitor 24"', // key = nombre: 'valor de la propiedad'
    precio: 300,
    disponible: true
}

// mostrando el objeto por consola
console.log(producto);